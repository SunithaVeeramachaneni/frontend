import { DynamicPreviewResponseTypeLoaderDirective } from './dynamic-preview-response-type-loader.directive';

describe('DynamicPreviewResponseTypeLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicPreviewResponseTypeLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
